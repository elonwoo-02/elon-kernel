<section class="post-markdown mt-8">
  <slot />
</section>

<style>
  .post-markdown {
    color: var(--app-text-muted);
    line-height: 1.85;
  }

  .post-markdown :global(h1),
  .post-markdown :global(h2),
  .post-markdown :global(h3),
  .post-markdown :global(h4) {
    color: var(--app-text);
    font-weight: 700;
    letter-spacing: -0.01em;
    line-height: 1.3;
    margin-top: 1.6em;
    margin-bottom: 0.65em;
    scroll-margin-top: 6.5rem;
  }

  .post-markdown :global(h1) {
    display: none;
  }

  .post-markdown :global(h2) {
    font-size: clamp(1.35rem, 1.2rem + 0.6vw, 1.75rem);
    padding: 0.5rem 0 0.5rem 0.85rem;
    border-left: 4px solid var(--app-accent);
    border-bottom: 1px solid color-mix(in srgb, var(--app-accent) 30%, transparent);
  }

  .post-markdown :global(h3) {
    font-size: clamp(1.1rem, 1rem + 0.5vw, 1.35rem);
    padding-left: 0.7rem;
    border-left: 2px solid color-mix(in srgb, var(--app-accent) 40%, transparent);
  }

  .post-markdown :global(p),
  .post-markdown :global(li) {
    font-size: 1rem;
  }

  .post-markdown :global(ul),
  .post-markdown :global(ol) {
    margin: 0.85rem 0 1rem;
    padding-left: 1.35rem;
  }

  .post-markdown :global(a) {
    color: var(--app-link);
    text-decoration: underline;
    text-underline-offset: 0.2em;
    transition: color 200ms ease;
  }

  .post-markdown :global(a:hover) {
    color: var(--app-accent-hover);
  }

  .post-markdown :global(code) {
    border: 1px solid color-mix(in srgb, var(--app-border) 90%, transparent);
    border-radius: 0.35rem;
    background: color-mix(in srgb, var(--app-surface-2) 75%, transparent);
    padding: 0.08rem 0.35rem;
    font-size: 0.92em;
  }

  .post-markdown :global(pre) {
    margin: 1.1rem 0;
    border: 1px solid color-mix(in srgb, var(--app-border) 95%, transparent);
    border-radius: 0.75rem;
    background: color-mix(in srgb, var(--app-bg) 78%, black);
    padding: 1rem;
    overflow-x: auto;
  }

  .post-markdown :global(pre code) {
    border: 0;
    border-radius: 0;
    background: transparent;
    padding: 0;
    color: var(--app-accent-contrast);
  }

  .post-markdown :global(blockquote) {
    margin: 1.5rem 0;
    padding: 0.85rem 1.25rem;
    border-left: 3px solid var(--app-accent);
    background: color-mix(in srgb, var(--app-accent-soft) 60%, transparent);
    border-radius: 0 0.4rem 0.4rem 0;
    color: var(--app-text);
    font-style: italic;
  }

  .post-markdown :global(img) {
    border-radius: 0.6rem;
    border: 1px solid color-mix(in srgb, var(--app-border) 70%, transparent);
    margin: 1.5rem auto;
    max-width: 100%;
    height: auto;
    display: block;
  }

  /* === Figure & Caption === */
  .post-markdown :global(figure) {
    margin: 2rem auto;
    text-align: center;
  }

  .post-markdown :global(figure img) {
    margin: 0 auto 0.75rem;
  }

  .post-markdown :global(figcaption) {
    font-size: 0.85rem;
    color: var(--app-text-muted);
    line-height: 1.5;
    padding: 0.5rem 1rem;
    background: color-mix(in srgb, var(--app-accent-soft) 40%, transparent);
    border-radius: 0.4rem;
    display: inline-block;
    border: 1px solid color-mix(in srgb, var(--app-accent) 20%, transparent);
  }

  /* === Tables === */
  .post-markdown :global(table) {
    width: 100%;
    border-collapse: collapse;
    margin: 1.5rem 0;
    overflow-x: auto;
    display: block;
    font-size: 0.92rem;
    border-radius: 0.5rem;
    border: 1px solid color-mix(in srgb, var(--app-border) 70%, transparent);
  }

  .post-markdown :global(th) {
    background: color-mix(in srgb, var(--app-accent) 12%, transparent);
    color: var(--app-text);
    font-weight: 600;
    font-size: 0.85rem;
    letter-spacing: 0.02em;
    text-transform: uppercase;
  }

  .post-markdown :global(th),
  .post-markdown :global(td) {
    border: 1px solid color-mix(in srgb, var(--app-border) 80%, transparent);
    padding: 0.6rem 0.85rem;
    text-align: left;
    vertical-align: top;
  }

  .post-markdown :global(tbody tr:nth-child(even)) {
    background: color-mix(in srgb, var(--app-surface-2) 30%, transparent);
  }

  .post-markdown :global(tbody tr:hover) {
    background: color-mix(in srgb, var(--app-accent-soft) 40%, transparent);
  }

  .post-markdown :global(hr) {
    border: none;
    height: 1px;
    background: color-mix(in srgb, var(--app-accent) 25%, transparent);
    margin: 2rem 0;
  }

  /* === Inline small text / keywords === */
  .post-markdown :global(strong) {
    color: var(--app-text);
    font-weight: 650;
  }
</style>
